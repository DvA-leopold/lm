package controller.commands;

import model.ProjectModel;
import view.ProjectView;

public final class AddMetadataCommand implements Command {
    @Override
    public void execute(ProjectModel model, ProjectView view) {

    }
}
